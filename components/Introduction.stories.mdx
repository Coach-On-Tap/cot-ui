import { Meta } from "@storybook/blocks";

<Meta title="Introduction" />

# Admin Portal Component Library

Welcome to the Admin Portal Component Library! This library provides a comprehensive set of React components designed for building modern admin dashboards and management interfaces.

## 🚀 Getting Started

This Storybook contains interactive examples of all available components. You can:

- Browse components by category in the sidebar
- View component properties and customize them using the Controls panel
- See component behavior with different props
- Copy code examples to use in your projects

## 📦 Available Components

### Form Components

- **Button** - Various button styles and states
- **Checkbox** - Custom checkboxes with different states
- **Field** - Form field wrapper with labels and validation
- **Select** - Custom dropdown select component
- **Switch** - Toggle switch component

### Layout Components

- **Card** - Container for content sections
- **Header** - Page header with navigation
- **Layout** - Main layout wrapper
- **Sidebar** - Navigation sidebar
- **Modal** - Modal dialogs and overlays

### Display Components

- **Icon** - SVG icon system
- **Image** - Optimized image component
- **Logo** - Brand logo component
- **Spinner** - Loading spinners
- **Tooltip** - Contextual tooltips

### Navigation

- **NavLink** - Navigation links with active states
- **Tabs** - Tab navigation component

### Data Components

- **Table** - Data tables with sorting and filtering
- **Search** - Search input component

## 🎨 Theme System

All components support light and dark themes through our ThemeProvider. The theme automatically applies appropriate colors, spacing, and styling.

## 🛠 Development

To contribute or modify components:

1. Each component has its own directory with an `index.tsx` file
2. Stories are defined in `ComponentName.stories.tsx` files
3. Components use Tailwind CSS for styling with custom design tokens

## 📖 Documentation

Each component story includes:

- Interactive examples
- Props documentation
- Usage guidelines
- Accessibility notes

Start exploring by selecting a component from the sidebar!
